#!/bin/bash
#
# ------------------------
# Open menu or create user
# ------------------------


if [ "$(whoami)" != "plexuser" ];
then

  bash /opt/GooPlex/install/createuser.sh

else

  bash /opt/GooPlex/menus/main.sh

fi
